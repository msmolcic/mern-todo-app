{
  "name": "server",
  "version": "1.0.0",
  "description": "Todo list application.",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Mario Smolčić",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^6.1.0",
    "express": "^4.16.4",
    "graphql-yoga": "^1.16.7",
    "mongoose": "^5.3.13"
  },
  "engines": {
    "node": "9.11.1"
  }
}
